V15 NEXT STEPS — PASTE THESE INSTRUCTIONS INTO CLAUDE CODE
============================================================

STEP 1: PUSH TO PRODUCTION
---------------------------
git push origin main

Then verify the deployment completes successfully.


STEP 2: TEST EXISTING CITIES (before doing anything else)
-----------------------------------------------------------
After deploy, verify these things work on the LIVE site:

A) prod_cities table is populated and the site uses it:
   - What does the city count show on the homepage?
   - Does /cities only show cities from prod_cities?
   - Do city pages for cities NOT in prod_cities handle gracefully?

B) The 17 fresh cities from the audit still have data flowing:
   - Pick 3-4 (Houston, Dallas, Chicago, Phoenix) and verify
     /permits/{city} shows recent permits
   - Check that the most recent permit dates are from this week

C) Newly reactivated bulk sources actually collect:
   - Trigger a manual collection run for Miami-Dade County
   - Trigger a manual collection run for Fort Worth
   - Verify permits appear in the DB after collection
   - If Miami-Dade fails: the city_field is set to "STNDADDR"
     which is a street address, not municipality name. This
     probably needs to be fixed — check what fields the ArcGIS
     endpoint actually returns for city/municipality.
   - If Fort Worth fails: the date_field is set to
     "Permit_Category" which is a PLACEHOLDER. Find the actual
     date field from the FeatureServer metadata.

D) scraper_runs logging works:
   - After running collections above, check scraper_runs table
   - Verify it logged the run with status, permits_found, duration

E) /admin/collector-health dashboard loads and shows status

FIX anything that's broken before moving on. The goal is:
every city currently in prod_cities has verified, working
data collection.


STEP 3: SEED prod_cities WITH ALL VERIFIED CITIES
---------------------------------------------------
The audit found 17 fresh + 47 slightly stale cities. Add all
cities that have collected data in the last 30 days to
prod_cities if they aren't already there. This is a DB migration:

  INSERT INTO prod_cities (city, state, city_slug, source_type,
    source_id, source_scope, verified_date, last_collection,
    total_permits, status, added_by)
  SELECT DISTINCT
    p.city,
    p.state,
    LOWER(REPLACE(p.city, ' ', '-')),
    'unknown',  -- will be updated as we verify sources
    NULL,
    'unknown',
    NOW(),
    MAX(p.created_at),
    COUNT(*),
    'active',
    'v15_migration'
  FROM permits p
  WHERE p.created_at >= NOW() - INTERVAL '30 days'
  GROUP BY p.city, p.state
  ON CONFLICT (city_slug) DO NOTHING;


STEP 4: BUILD THE DISCOVERY ENGINE AS A PYTHON SCRIPT
------------------------------------------------------
⚠️  THIS IS CRITICAL: The discovery engine must be a standalone
Python script that runs WITHOUT Claude/AI. No LLM calls, no
web searches through Claude agents. Just direct API calls to
Socrata, ArcGIS, and CKAN.

Create: discovery_engine.py

This script should:

  1. Read the us_cities table (30k cities, ordered by population)

  2. For each city, skip if already in prod_cities

  3. Check these APIs directly (no AI needed):

     SOCRATA CHECK:
       GET https://api.us.socrata.com/api/catalog/v1
         ?q={city_name} building permits
         &domains=data.{city}.gov
         &limit=5
       If results contain a dataset with "permit" in the name,
       test it: GET the dataset endpoint with $limit=1
       If it returns recent data → log as "source found"

     ARCGIS CHECK:
       Search the ArcGIS Hub:
       GET https://hub.arcgis.com/api/v3/search
         ?q={city_name} building permits
         &filter[type]=Feature Service
       If results found, test the FeatureServer:
       GET {service_url}/0/query?where=1=1&resultRecordCount=1&f=json
       If it returns data → log as "source found"

     CKAN CHECK:
       GET https://catalog.data.gov/api/3/action/package_search
         ?q={city_name} building permits
         &rows=5
       If results contain permit datasets → test and log

  4. For each discovered source, write a row to a discovery_log
     table (or CSV file):
       city, state, source_type, endpoint_url, has_recent_data,
       field_names, record_count, checked_at

  5. DO NOT automatically add to prod_cities. Just log what it
     finds. We review the log and add manually (or with a
     separate approval step).

  6. Include rate limiting: sleep 2 seconds between API calls
     to avoid getting blocked.

  7. Include a progress tracker: save which city it last checked
     so it can resume if interrupted. Don't restart from scratch.

  8. Print progress: "Checked 1500/30000 cities. Found 45 sources.
     Currently checking: Sacramento, CA"

The script should be runnable with:
  python discovery_engine.py

And should complete its full pass through 30k cities in
~24-48 hours of runtime (with rate limiting).

ALSO check state-level and county-level sources FIRST (as
described in COLLECTOR_REDESIGN.txt phases 1-2). One state
source can cover hundreds of cities, one county source can
cover dozens. Check these before doing individual city lookups.

This is NOT an AI task. This is a straightforward Python script
that makes HTTP requests to public APIs. No tokens, no credits,
no Claude agents needed.


STEP 5: FIX THE COLLECTION LOOP
---------------------------------
The current scheduled_collection() needs to iterate prod_cities
instead of CITY_REGISTRY. Update it:

  def scheduled_collection():
      """V15: Collect from prod_cities list."""
      time.sleep(300)  # Wait 5 min after startup
      while True:
          cities = get_prod_cities()  # SELECT * FROM prod_cities WHERE status='active'
          for city in cities:
              try:
                  collect_city(city)
                  log_run(city, 'success', permits_found)
              except Exception as e:
                  log_run(city, 'error', 0, str(e))
                  handle_failure(city)  # pause after 3 consecutive failures
          time.sleep(21600)  # 6 hours

Remove the file-based failure tracker. Replace with the
paused/retry logic described in COLLECTOR_REDESIGN.txt Phase 5.


PRIORITY ORDER:
  Step 1 (push) → Step 2 (test) → Step 5 (fix collection loop)
  → Step 3 (seed prod_cities) → Step 4 (discovery script)

Steps 1-3 and 5 should be done today.
Step 4 (discovery engine) can be a separate commit/task.
