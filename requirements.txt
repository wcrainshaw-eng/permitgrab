flask==3.0.0
requests==2.31.0
gunicorn==21.2.0
apscheduler==3.10.4
stripe==7.0.0
flask-limiter==3.5.0
flask-login==0.6.3
werkzeug==3.0.1
markdown==3.5.1
bcrypt==4.1.2
Flask-SQLAlchemy==3.1.1
psycopg2-binary==2.9.9
pytz==2024.1
beautifulsoup4==4.12.3
psutil>=5.9
flask-compress>=1.14
# V508: Playwright-based Accela CapDetail scraper for JS-SPA portals
# (SBCO and similar) where static HTML returns an empty shell. Chromium
# is installed via `playwright install --with-deps chromium` in the
# Dockerfile build step.
playwright==1.45.0
# duckduckgo-search removed — causes build failures on Render, search done locally instead
# V229 B1: removed V227's httpx/selectolax/tenacity/pybreaker too — they
# only supported enrichment_engine.py which was never imported by server.py
# or the daemon, so both the module and its deps are dead weight.
