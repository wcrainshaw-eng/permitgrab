flask==3.0.0
requests==2.31.0
gunicorn==21.2.0
apscheduler==3.10.4
stripe==7.0.0
flask-limiter==3.5.0
werkzeug==3.0.1
markdown==3.5.1
bcrypt==4.1.2
Flask-SQLAlchemy==3.1.1
psycopg2-binary==2.9.9
pytz==2024.1
beautifulsoup4==4.12.3
psutil>=5.9
# duckduckgo-search removed — causes build failures on Render, search done locally instead
# V227: production-grade resilience libs (all pure Python, no C extensions).
# duckduckgo-search intentionally NOT re-added — same Render build failure as
# before. enrichment_engine.py uses httpx directly against html.duckduckgo.com
# instead, which is what the old FreeEnrichmentEngine already did.
httpx>=0.27.0
selectolax>=0.3.21
tenacity>=8.2.0
pybreaker>=1.2.0
